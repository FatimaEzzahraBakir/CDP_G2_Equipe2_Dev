<!DOCTYPE html>
<html lang="fr" dir="ltr">

<head>
  <meta charset="utf-8">
  <link rel="stylesheet" href="/css/materialize.css">
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <% if(typeof project == 'undefined'){ %>
    <title>Untitled</title>
    <% } else { %>
      <title><%= project.name %></title>
      <% } %>
    </head>

    <body>
      <nav class="navmargin">
        <div class="nav-wrapper">
          <a href="/" class="brand-logo">Gestion projets</a>
        </div>
      </nav>
      <div class="container">
        <div class="row">
          <div class="col s12 m4 l2">
          <a class="waves-effect waves-light btn orange lighten-2" href="/user/<%= user %>/projects">
          <i class="material-icons left">keyboard_backspace</i>retour</a>
          </div>
        </div>
          <div class="row">
            <div class="col s12 m4 l8">
              <% if(typeof project != 'undefined') { %>
                <div class="row">
                  <div class="card z-depth-1">
                    <div class="card-content">
                      <h6 class="card-title"><%= project.name %></h6>

                      <p><%= project.description %></p>
                    </div>
                    <div class="card-action">
                      <a href="/user/<%= user %>/projects/<%= project.name %>/modify" class="center-align">Modifier</a>
                      <a href="/user/<%= user %>/projects/<%= project.name %>/delete" class="right materialize-red-text">Supprimer</a>
                    </div>
                  </div>
                </div>
                <% } %>

              </div>
            </div>
            <div class="row">
              <div class="col s12 m4 l2">
                <%// if(typeof members != 'undefined') { %>
                  <div class="row">
                    <div class="card z-depth-1">
                      <div class="card-content">
                        <h6 class="card-title">Membres</h6>
                        <ul>
                          <%// members.forEach(function(member) { console.log(member); %>
                            <li><%// member.login %> </li>
                            <%// }); %>
                          </ul>
                        </div>
                        <div class="card-action">
                          <a href="/user/<%= user %>/projects/<%= project.name %>/addMember" class="center-align green-text accent-4">
                            <i class="material-icons left">add</i>Ajouter un membre</a>
                          </div>
                        </div>
                      </div>
                      <% //} %>

                    </div>
                  </div>
                </div>

              </body>

              </html>
